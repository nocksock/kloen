{
  "name": "kloenen",
  "description": "a super tiny and simple pubsub",
  "homepage": "https://git.sr.ht/~snock/kloenen",
  "version": "0.3.0",
  "type": "module",
  "files": [
    "dist",
    "index.d.ts"
  ],
  "module": "./lib/main.js",
  "exports": {
    "types": "./index.d.ts",
    "import": "./lib/main.js",
    "release:minor": "pnpm version minor && pnpm publish --go-git-checks",
    "release:patch": "pnpm version patch && pnpm publish --go-git-checks"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.2.10"
  },
  "dependencies": {
    "kloenen": "link:",
    "vitest": "^1.6.0"
  }
}
